<?xml version="1.0" encoding="UTF-8"?>

	
	<!-- (준수) 일반검진 체크리스트 추가 아직안씀--> 
	<entry key ="insertCheckList">
	
	INSERT
		INTO CHECK_LIST
		(
		  USER_NO
		, MEDI_LIST
		, SURGERY_NAME
		, SURGERY_YN
		, FLY_YN
    , CHECKUP_NO


		)

		VALUES
		(
		  ?
		, ?
		, ?
		, ?
		, ?
		, (SELECT APPOINTMENT_NO 
							FROM CHECKUP_APP 
							WHERE USER_NO = ?
							AND TRUNC(APPOINTMENT_DATE) = TRUNC(SYSDATE))
		)
	
	</entry>